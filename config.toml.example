# vps-monitor configuration

# Bind address — MUST be a loopback / private IP. Public binds (0.0.0.0, ::, public IPs) are rejected at startup.
bind = "127.0.0.1:8443"

# SQLite database path
db_path = "/var/lib/vps-monitor/db.sqlite"

# Session lifetime in seconds (default: 7 days)
session_ttl_secs = 604800

# Audit log retention in days
audit_retention_days = 90

# Failed-login lockout
max_failed_logins = 5
lockout_minutes = 15

# PM2 home directories to scan (one per user that runs PM2).
# When empty, vps-monitor auto-discovers /root/.pm2 and /home/*/.pm2.
# Make sure the vps-monitor user has read+execute access on these dirs:
#   sudo setfacl -R -m u:vps-monitor:rX /root/.pm2
pm2_homes = []

# Optional: override default docker socket path
# docker_socket = "/var/run/docker.sock"
